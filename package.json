{
  "name": "app",
  "version": "1.0.0",
  "description": "1. application can be web-service (REST API) or web application",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
